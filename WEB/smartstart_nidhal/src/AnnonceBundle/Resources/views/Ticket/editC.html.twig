{% extends 'baseClient.html.twig' %}
{%  block content %}
    <center>
    <h>Update a complaint</h>

{{ form_start(f) }}

    Offre:*  {{ form_widget(f.offre) }}</br>
    Service:*  {{ form_widget(f.service) }}</br>
    Category:*  {{ form_widget(f.categorie) }}</br>
    Reason of the complaint:*  {{ form_widget(f.motifreclamation) }}</br>

    {{ form_widget(f.save) }}
{{ form_end(f) }}</br>
    </center>

{% endblock %}